Ok, I think for let's do this:
```
User types "Use createInvite() and make an invite for Opix"
           ↓
ChatGPT sends a request to the app using MCP
           ↓
The MCP does the request and give back a JSON data and a a status code(403 means the key is invalid, also, Users need a key for it to work)
           ↓
ChatGPT Tells the user how it went
```
